services:
  winet-extractor:
    restart: unless-stopped
    image: winet-extractor
    build:
      context: .
    volumes:
      - ./env:/usr/src/app/.env:ro
    network_mode: host
    environment:
      - TZ=Europe/Berlin
